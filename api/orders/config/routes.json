{
    "routes": [
        {
            "method": "POST",
            "path": "/orders",
            "handler": "orders.getOrder",
            "config": {
                "policies": []
            }
        }
    ]
}